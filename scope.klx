x := 0;
y := 10; // Outer y

zero := 0;

While x < 5
  y := 10 * x; // Inner y

  If x == 0
    zero := 0;
    Red Circle At (y, zero);
  Else
    zero := 0;
    Blue Circle At (y, zero);
  Endif;

  x = x + 1;
Endwhile;

Green Diamond At (y, y);
